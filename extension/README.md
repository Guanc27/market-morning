# Market Morning Extension

The Chrome side panel loads from **`dist/`** (plain HTML/CSS/JS). Edit files in `dist/` directly.

The legacy React scaffold under `src/` was removed — do not run `vite build` unless you reintroduce a build pipeline.

Load unpacked extension from this folder in Chrome (`dist/manifest.json`).
