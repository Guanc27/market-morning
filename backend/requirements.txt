# Direct dependencies, pinned to exact versions for reproducible installs.
# Versions reflect what is installed and verified in backend/.venv.
# The full resolved dependency graph (transitive deps) is locked in requirements.lock.
# To regenerate the lock after changing a pin:  pip install -r requirements.txt && pip freeze > requirements.lock
fastapi==0.139.0
uvicorn[standard]==0.49.0
anthropic==0.115.1
financetoolkit==2.1.3
yfinance==1.5.1
pandas==3.0.3
numpy==2.5.0
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1
python-multipart==0.0.32
aiosqlite==0.22.1
mcp==1.28.1
feedparser==6.0.12
